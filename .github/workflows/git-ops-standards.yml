name: git-ops-standards

on:
  pull_request:

jobs:
  gate:
    permissions:
      contents: read
      pull-requests: read
    uses: SocioProphet/git-ops-standards/.github/workflows/pr-merge-gate.yml@main
